import 'package:flutter/material.dart';

class SupportMessageScreen extends StatelessWidget {
  const SupportMessageScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('応援メッセージ画面'));
  }
}
